export const preventOverflow = {
    name: 'preventOverflow',
    options: {
        altAxis: true,
        rootBoundary: 'document',
        boundary: document.body,
    },
}

export const flip = {
    name: 'flip',
    options: {
        rootBoundary: 'document',
        boundary: document.body,
    },
}
